# Infrastructure & PowerSync Setup Implementation Plan

## Overview
Set up the local development environment infrastructure, including a PostgreSQL database and the PowerSync sync service, orchestrated via Docker Compose.

## Scope Definition (CRITICAL)
### In Scope
- Create `docker-compose.yml` in the root.
- Configure PostgreSQL service.
- Configure PowerSync sync service.
- Create initial PowerSync configuration (`powersync.yaml`).
- Set up necessary environment variables.
### Out of Scope (DO NOT TOUCH)
- Connecting the mobile app to PowerSync (Phase 2).
- Connecting the API to Postgres (except for verification).
- Auth implementation.

## Current State Analysis
No infrastructure or Docker configuration exists. Root directory is ready for integration.

## Implementation Phases

### Phase 1: Docker Compose Scaffolding
- **Goal**: Define the service orchestration.
- **Steps**:
  1. [x] Create root `docker-compose.yml`.
  2. [x] Add `postgres` service with health checks and persistent volume.
  3. [x] Add `powersync` service depending on `postgres`.
- **Verification**: Run `docker compose config` to ensure valid YAML.

### Phase 2: PowerSync Configuration
- **Goal**: Configure the sync service.
- **Steps**:
  1. [x] Create `powersync.yaml` with connection details to Postgres.
  2. [x] Define initial sync rules (placeholder).
- **Verification**: Ensure file exists and is valid YAML.

### Phase 3: Infrastructure Bootstrapping (UNVERIFIED - Docker not found)
- **Goal**: Start the services and verify connectivity.
- **Steps**:
  1. [ ] Run `docker compose up -d`.
  2. [ ] Verify Postgres is accepting connections.
  3. [ ] Verify PowerSync service started successfully (check logs).
- **Verification**: `docker compose ps` shows all services as `running`.
