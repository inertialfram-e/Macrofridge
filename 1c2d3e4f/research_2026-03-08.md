# Research: Infrastructure & PowerSync Setup

**Date**: 2026-03-08

## 1. Executive Summary
The project lacks any local infrastructure (Postgres, PowerSync). No `docker-compose.yml` or database configuration files exist in the root or `apps/api`.

## 2. Technical Context
- **Affected components**: Root directory.
- **Current behavior**: No local development database or sync service is configured.
- **Logic and data flow**: N/A.

## 3. Findings & Analysis
- **Monorepo State**: Scaffolding complete. Ready for infrastructure integration.
- **Database Requirements**:
  - PostgreSQL (Primary DB).
  - PowerSync Sync Service (requires connection to Postgres).
- **Configuration Needs**:
  - `docker-compose.yml` for service orchestration.
  - Environment variables for database credentials.
  - PowerSync configuration file (e.g., `powersync.yaml`).

## 4. Technical Constraints
- Requires Docker and Docker Compose to be installed on the host.
- PowerSync requires a specific schema and replication setup in Postgres.

## 5. Architecture Documentation
The architecture relies on a central Postgres database that synchronizes with local SQLite databases on mobile devices via the PowerSync service.
