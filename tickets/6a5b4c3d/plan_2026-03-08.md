# Initialize Monorepo and Shared Configs Implementation Plan

## Overview
Establish the foundational monorepo structure using Turborepo and pnpm, providing a unified environment for the mobile and API applications with shared linting, formatting, and TypeScript configurations.

## Scope Definition (CRITICAL)
### In Scope
- Initialize `package.json` at the root.
- Configure `pnpm-workspace.yaml`.
- Set up `turbo.json`.
- Create `packages/config/eslint-preset`.
- Create `packages/config/prettier-preset`.
- Create `packages/config/typescript-config`.
### Out of Scope (DO NOT TOUCH)
- Scaffolding `apps/mobile` or `apps/api` (Ticket `7e8f9a0b`).
- Docker or PowerSync setup (Ticket `1c2d3e4f`).
- Auth implementation (Ticket `9a8b7c6d`).

## Current State Analysis
The root directory `/Users/inertialframe/Documents/Macrofridge` contains only documentation and ticket directories. No Node.js or monorepo infrastructure exists.

## Implementation Phases

### Phase 1: Monorepo Scaffolding
- **Goal**: Initialize the root workspace and Turborepo.
- **Steps**:
  1. [x] Create root `package.json` with basic metadata and `pnpm` engines.
  2. [x] Create `pnpm-workspace.yaml` defining `apps/*` and `packages/*`.
  3. [x] Create `turbo.json` with standard pipelines (`build`, `lint`, `dev`).
  4. [x] Create `.gitignore` for Node.js and Turborepo artifacts.
- **Verification**: Run `pnpm install` (though empty) and ensure `turbo` is recognized.

### Phase 2: Shared Configurations
- **Goal**: Create reusable configuration packages.
- **Steps**:
  1. [x] Create `packages/config/package.json` (as a meta-package or individual packages).
  2. [x] Implement `packages/config/eslint-preset/index.js` (base config).
  3. [x] Implement `packages/config/prettier-preset/index.json`.
  4. [x] Implement `packages/config/typescript-config/base.json`.
- **Verification**: Ensure files exist and are valid JSON/JS.

### Phase 3: Root Linkage
- **Goal**: Link root scripts to Turborepo.
- **Steps**:
  1. [x] Add `build`, `lint`, and `dev` scripts to root `package.json` using `turbo`.
- **Verification**: `npm run build` should execute via Turbo (even if 0 tasks).
